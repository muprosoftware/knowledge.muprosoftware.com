---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<article class="flex" aria-labelledby="about-astro-heading">
  <small id="about-astro-heading">
    {title}
    <span class="sr-only">Astro</span>
  </small>
  <img src="mupro-leftright.png" width="200px"/>
  <slot />
</article>

<style>
  article {
    max-width: 40rem;
    margin-inline: auto;
    padding-block: 5rem;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.5rem;
  }
  article > :global(*) {
    margin-top: 0 !important;
    max-width: 50ch;
  }
  small {
    color: var(--sl-color-gray-3);
  }
  svg {
    width: 15rem;
  }
</style>